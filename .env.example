# CoinGecko Demo API key (free tier, registration required at coingecko.com/en/developers/dashboard).
# Without it the client falls back to the unauthenticated public endpoint, which is more
# aggressively rate-limited and can throttle without warning.
CG_API_KEY=
