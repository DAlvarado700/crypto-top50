__pycache__/
*.pyc
.venv/
venv/
.env
data/*.db-journal
output/*.json
output/*.csv
.pytest_cache/

# data/tracker.db and dashboard/data.js ARE tracked on purpose: the daily GitHub
# Actions workflow commits them, so git history doubles as an audit trail of every
# day's top-50 snapshot, and the dashboard on GitHub Pages always serves fresh data.
